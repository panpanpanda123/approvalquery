# 日常维护指南

## 🎯 你需要知道的核心概念

### Git的三个区域

```
工作区 (你修改的文件)
    ↓ git add
暂存区 (准备提交的文件)
    ↓ git commit
本地仓库 (你电脑上的版本)
    ↓ git push
远程仓库 (GitHub上的版本)
```

### 服务器 vs 本地

- **本地（你的电脑）**：开发和修改代码
- **GitHub（远程仓库）**：代码的中转站
- **服务器（云服务器）**：运行网站的地方

**流程：** 本地修改 → push到GitHub → 服务器pull下来

---

## 📋 日常操作场景

### 场景1：只更新Excel数据（最常用）⭐

**不需要改代码，只更新数据**

```bash
# 方法1：Windows上传（推荐）
双击 简易更新.bat

# 方法2：直接在服务器操作
ssh root@139.224.200.133
cd /var/www/approval-viewer/approvalquery
python3 parse_excel.py
chmod 644 approval_data.json
```

**注意：** 这种情况不需要用Git！

---

### 场景2：修改了代码（偶尔）

**比如修改了 parse_excel.py 或 index.html**

#### 在本地（你的电脑）：

```bash
# 1. 查看修改了什么
git status

# 2. 添加修改的文件
git add .

# 3. 提交修改（写个说明）
git commit -m "修改了XXX功能"

# 4. 推送到GitHub
git push
```

#### 在服务器：

```bash
# SSH登录
ssh root@139.224.200.133

# 进入目录
cd /var/www/approval-viewer/approvalquery

# 拉取最新代码
git pull

# 如果有冲突，用这个（放弃服务器上的修改）
git reset --hard origin/master
```

---

## ⚠️ 常见问题和解决方法

### 问题1：服务器上 git pull 提示有未提交的修改

**错误信息：**
```
error: cannot pull with rebase: You have unstaged changes.
error: please commit or stash them.
```

**原因：** 你在服务器上直接修改了文件

**解决方法（3选1）：**

**方法1：放弃服务器上的修改（推荐）**
```bash
git reset --hard origin/master
```
这会丢弃服务器上的所有修改，使用GitHub上的最新版本。

**方法2：暂存修改**
```bash
git stash        # 暂存修改
git pull         # 拉取最新代码
git stash pop    # 恢复修改（可能有冲突）
```

**方法3：提交修改**
```bash
git add .
git commit -m "服务器上的修改"
git pull
```

**建议：** 永远不要在服务器上直接修改代码！只在本地修改。

---

### 问题2：git pull 提示冲突

**错误信息：**
```
fatal: Not possible to fast-forward, aborting.
```

**原因：** 服务器和GitHub的代码不一致

**解决方法：**
```bash
# 强制使用GitHub上的版本
git fetch origin
git reset --hard origin/master
```

---

### 问题3：不小心在服务器上修改了文件

**查看修改了什么：**
```bash
git status
```

**放弃所有修改：**
```bash
git reset --hard HEAD
```

**只放弃某个文件的修改：**
```bash
git checkout -- 文件名
```

---

## 🔄 标准工作流程

### 日常更新数据（90%的情况）

```bash
# 在服务器上
ssh root@139.224.200.133
cd /var/www/approval-viewer/approvalquery
python3 parse_excel.py
chmod 644 approval_data.json
```

**不需要Git！**

---

### 修改代码后部署（10%的情况）

#### 步骤1：本地开发

```bash
# 在你的电脑上
cd approval-viewer

# 修改代码...

# 提交
git add .
git commit -m "说明你改了什么"
git push
```

#### 步骤2：服务器更新

```bash
# SSH登录
ssh root@139.224.200.133

# 进入目录
cd /var/www/approval-viewer/approvalquery

# 拉取最新代码（如果有问题用下面的命令）
git pull

# 如果git pull失败，用这个
git fetch origin
git reset --hard origin/master

# 重新生成数据
python3 parse_excel.py
chmod 644 approval_data.json
```

---

## 💡 最佳实践

### ✅ 应该做的

1. **只在本地修改代码**
   - 在你的电脑上用编辑器修改
   - 测试没问题后再push

2. **服务器只用来运行**
   - 只运行 `python3 parse_excel.py`
   - 只运行 `git pull`
   - 不要直接编辑文件

3. **定期备份Excel文件**
   - 脚本会自动备份
   - 保留原始文件

4. **提交时写清楚说明**
   ```bash
   git commit -m "修复NaN问题"  # 好
   git commit -m "update"       # 不好
   ```

### ❌ 不应该做的

1. **不要在服务器上直接修改代码**
   - 会导致Git冲突
   - 难以追踪修改

2. **不要随便用 git merge**
   - 容易搞乱
   - 用 `git reset --hard` 更安全

3. **不要删除 .git 目录**
   - 会丢失所有版本历史

---

## 🆘 救命命令

### 服务器上搞乱了，想重新开始

```bash
cd /var/www/approval-viewer/approvalquery
git fetch origin
git reset --hard origin/master
```

这会让服务器完全同步GitHub上的版本。

### 本地搞乱了，想重新开始

```bash
cd approval-viewer
git fetch origin
git reset --hard origin/master
```

### 查看当前状态

```bash
git status          # 查看修改了什么
git log --oneline   # 查看提交历史
git diff            # 查看具体修改内容
```

---

## 📝 快速参考

### 日常更新数据
```bash
ssh root@139.224.200.133
cd /var/www/approval-viewer/approvalquery
python3 parse_excel.py
chmod 644 approval_data.json
```

### 代码有更新
```bash
# 本地
git add .
git commit -m "说明"
git push

# 服务器
ssh root@139.224.200.133
cd /var/www/approval-viewer/approvalquery
git reset --hard origin/master
python3 parse_excel.py
```

### 遇到问题
```bash
# 服务器上
git reset --hard origin/master
```

---

## 🎓 Git术语解释

- **pull**: 从GitHub下载最新代码
- **push**: 把本地代码上传到GitHub
- **commit**: 保存一个版本
- **add**: 把修改加入暂存区
- **reset**: 重置到某个版本
- **stash**: 暂时保存修改
- **origin**: GitHub远程仓库的别名
- **master**: 主分支名称

---

## 🔑 记住这个原则

**服务器 = 只读**
- 只用来运行代码
- 只用来更新数据
- 不要修改代码

**本地 = 开发**
- 修改代码
- 测试功能
- 提交到GitHub

**GitHub = 中转站**
- 连接本地和服务器
- 保存所有版本
- 团队协作

---

## 📞 遇到问题怎么办

1. **先看错误信息**
   - 通常会告诉你怎么做

2. **用救命命令**
   ```bash
   git reset --hard origin/master
   ```

3. **实在不行，重新clone**
   ```bash
   cd /var/www/approval-viewer
   rm -rf approvalquery
   git clone <你的仓库地址> approvalquery
   ```

4. **数据不会丢**
   - Excel文件有备份
   - JSON可以重新生成

---

记住：**99%的时候你只需要运行 `python3 parse_excel.py`！**
