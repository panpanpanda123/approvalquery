# 🚀 立即执行清单

## 第一步：本地提交代码

```bash
git add .
git commit -m "智能检测nginx配置，清理项目文件"
git push
```

## 第二步：服务器操作

### 1. SSH登录
```bash
ssh root@139.224.200.133
```

### 2. 进入项目目录
```bash
cd /var/www/approval-viewer/approvalquery
```

### 3. 拉取最新代码
```bash
git pull
```

### 4. 设置脚本权限
```bash
chmod +x *.sh
```

### 5. 智能检测并配置nginx
```bash
bash 检测并配置nginx.sh
```

**这个脚本会：**
- 🔍 自动搜索 /var/www, /opt, /home 目录
- 📋 找到所有项目（approvalquery, kart, wuliu, weeklycheck）
- 📄 显示每个项目的路径和大小
- ⚙️ 生成统一的nginx配置
- 💾 自动备份旧配置
- ✅ 测试并重启nginx

**脚本会询问你：**
1. 是否生成新配置？→ 输入 `y`
2. 配置正确吗？→ 检查后输入 `y`

### 6. 修复数据加载
```bash
bash 修复数据加载.sh
```

## 第三步：浏览器验证

访问所有项目，按 `Ctrl+F5` 强制刷新：

**域名访问：**
- http://blitzepanda.top/approvalquery/
- http://blitzepanda.top/kart/
- http://blitzepanda.top/wuliu/
- http://blitzepanda.top/weeklycheck/

**IP访问：**
- http://139.224.200.133/approvalquery/
- http://139.224.200.133/kart/
- http://139.224.200.133/wuliu/
- http://139.224.200.133/weeklycheck/

## 预期结果

✅ 所有项目都能正常访问
✅ 域名和IP都能用
✅ approvalquery 数据正常显示
✅ nginx配置清晰统一

## 如果遇到问题

### 问题1：某个项目找不到

脚本会显示"未找到"，手动搜索：
```bash
find /var/www /opt /home -name "index.html" -type f 2>/dev/null
```

把结果发给我。

### 问题2：nginx配置测试失败

```bash
# 查看错误
sudo nginx -t

# 查看日志
sudo tail -50 /var/log/nginx/error.log
```

脚本会自动恢复备份，不用担心。

### 问题3：数据加载失败

```bash
bash 修复数据加载.sh
```

如果还不行，按 F12 查看浏览器控制台错误。

## 完成后

所有项目统一在一个nginx配置文件中：
- 配置文件：`/etc/nginx/sites-available/default`
- 备份位置：`/root/nginx_backup_日期时间/`

以后添加新项目，只需再运行一次：
```bash
bash 检测并配置nginx.sh
```

就这么简单！🎉
