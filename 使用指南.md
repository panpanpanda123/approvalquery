# 使用指南

## 📤 更新数据

### Windows用户（推荐）

1. 双击 `一键更新数据.bat`
2. 拖入新的Excel文件
3. 按回车

**首次使用需安装：**
- Python: https://www.python.org/downloads/
- Git for Windows: https://git-scm.com/download/win

### 服务器端更新

```bash
# 上传Excel
scp 新审批数据.xlsx root@服务器IP:/var/www/approval-viewer/approvalquery/

# SSH登录
ssh root@服务器IP

# 更新数据
cd /var/www/approval-viewer/approvalquery
bash daily_update.sh 新审批数据.xlsx
```

## 🔧 故障排查

### 数据加载失败

```bash
ssh root@服务器IP
cd /var/www/approval-viewer/approvalquery
bash 修复数据加载.sh
```

### nginx配置问题

```bash
# 智能检测并配置所有项目
bash 检测并配置nginx.sh

# 测试配置
sudo nginx -t

# 重启nginx
sudo systemctl reload nginx
```

## 🌐 nginx多项目管理

智能检测并配置所有项目：

```bash
bash 检测并配置nginx.sh
```

脚本会自动：
- 搜索 /var/www, /opt, /home 目录
- 检测所有项目位置
- 生成统一配置
- 支持域名和IP访问
- 备份旧配置
- 检测所有项目位置
- 生成统一配置
- 支持域名和IP访问
- 备份旧配置

## 📞 常见问题

**Q: 页面显示"数据加载失败"？**
```bash
bash 修复数据加载.sh
```

**Q: 如何添加新项目？**

把新项目放到 /var/www, /opt 或 /home 目录下，然后运行：
```bash
bash 检测并配置nginx.sh
```

脚本会自动检测并添加配置。

**Q: 如何查看nginx日志？**
```bash
sudo tail -50 /var/log/nginx/error.log
```
