# 使用指南

## 📤 更新数据

### Windows用户（推荐）

**方法1：简易更新（最简单）⭐**

1. 把新Excel改名为 `线上建店审批.xlsx`
2. 放到 `简易更新.bat` 同一目录
3. 双击运行

**方法2：灵活更新**

1. 把新Excel放到 `一键更新数据.bat` 同一目录
2. 双击运行
3. 输入文件名

**首次使用需安装：**
- Python: https://www.python.org/downloads/
- Git for Windows: https://git-scm.com/download/win

**SSH密钥登录：**
- 密钥需在 `C:\Users\你的用户名\.ssh\id_rsa`
- 详见 `SSH密钥配置.md`

### 服务器端更新

```bash
# 上传Excel
scp 新审批数据.xlsx root@服务器IP:/var/www/approval-viewer/approvalquery/

# SSH登录
ssh root@服务器IP

# 更新数据
cd /var/www/approval-viewer/approvalquery
bash daily_update.sh 新审批数据.xlsx
```

## 🔧 故障排查

### 数据加载失败

```bash
ssh root@服务器IP
cd /var/www/approval-viewer/approvalquery
bash 修复数据加载.sh
```

### nginx配置问题

```bash
# 智能检测并配置所有项目
bash 检测并配置nginx.sh

# 测试配置
sudo nginx -t

# 重启nginx
sudo systemctl reload nginx
```

## 🌐 nginx多项目管理

智能检测并配置所有项目：

```bash
bash 检测并配置nginx.sh
```

脚本会自动：
- 搜索 /var/www, /opt, /home 目录
- 检测所有项目位置
- 生成统一配置
- 支持域名和IP访问
- 备份旧配置

## 📞 常见问题

**Q: 页面显示"数据加载失败"？**
```bash
bash 修复数据加载.sh
```

**Q: Windows上传失败？**

检查SSH连接：
```bash
ssh root@服务器IP
```

如果需要密码或连接失败，查看 `SSH密钥配置.md`

**Q: 如何添加新项目？**

把新项目放到 /var/www, /opt 或 /home 目录下，然后：
```bash
bash 检测并配置nginx.sh
```

**Q: 如何查看nginx日志？**
```bash
sudo tail -50 /var/log/nginx/error.log
```

## 🚀 快速参考

**更新数据：**
- Windows: 双击 `简易更新.bat`
- 服务器: `bash daily_update.sh 文件.xlsx`

**修复问题：**
- 数据加载: `bash 修复数据加载.sh`
- nginx配置: `bash 检测并配置nginx.sh`

**访问地址：**
- http://blitzepanda.top/approvalquery
- http://139.224.200.133/approvalquery
