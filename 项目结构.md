# 项目结构说明

```
approval-viewer/
├── index.html                  # 前端页面
├── parse_excel.py             # Excel数据解析脚本
├── approval_data.json         # 生成的JSON数据（自动生成）
├── 线上建店审批.xlsx           # 源Excel文件
├── requirements.txt           # Python依赖
│
├── 一键更新数据.bat            # Windows更新工具
├── daily_update.sh            # 服务器端更新脚本
├── 修复数据加载.sh            # 数据加载问题修复
├── 检测并配置nginx.sh         # 智能检测并配置nginx
├── 服务器部署.sh              # 快速部署脚本
│
├── README.md                  # 项目说明
├── 使用指南.md                # 使用指南
├── 项目结构.md                # 本文件
└── .gitignore                 # Git忽略规则
```

## 文件说明

### 核心文件

**index.html**
- 前端可视化页面
- 使用原生JavaScript，无需构建
- 自动加载 approval_data.json

**parse_excel.py**
- 解析企业微信审批Excel
- 提取9个关键审批节点
- 生成 approval_data.json

**approval_data.json**
- 前端需要的数据文件
- 由 parse_excel.py 自动生成
- 包含统计数据和门店列表

### 脚本工具

**一键更新数据.bat** (Windows)
- 双击运行
- 自动解析Excel并上传到服务器
- 适合不懂代码的人使用

**daily_update.sh** (Linux)
- 服务器端数据更新
- 自动备份旧文件
- 清理7天前的备份

**修复数据加载.sh**
- 修复"数据加载失败"问题
- 重新生成数据
- 设置正确权限

**检测并配置nginx.sh**
- 智能检测所有Web项目
- 自动搜索 /var/www, /opt, /home
- 生成统一nginx配置
- 支持域名和IP访问
- 自动备份旧配置

**服务器部署.sh**
- 首次部署使用
- 安装依赖
- 生成数据
- 配置nginx

## 工作流程

### 首次部署

```bash
git clone <repo>
cd approval-viewer
bash 服务器部署.sh
```

### 日常更新

**Windows:**
```
双击 一键更新数据.bat
```

**Linux:**
```bash
bash daily_update.sh 新Excel文件.xlsx
```

### 故障修复

```bash
bash 修复数据加载.sh
```

## nginx配置

所有项目统一在 `/etc/nginx/sites-available/default` 中配置：

```nginx
location /approvalquery {
    alias /var/www/approval-viewer/approvalquery;
    index index.html;
    try_files $uri $uri/ /approvalquery/index.html;
}
```

支持：
- http://blitzepanda.top/approvalquery/
- http://139.224.200.133/approvalquery/

## 数据流程

```
线上建店审批.xlsx
    ↓ (parse_excel.py)
approval_data.json
    ↓ (fetch API)
index.html (浏览器显示)
```

## 权限要求

```bash
# 文件权限
chmod 644 *.html *.json
chmod 755 *.sh

# 所有者
chown www-data:www-data /var/www/approval-viewer/approvalquery
```

## 依赖

- Python 3.x
- openpyxl (Excel解析)
- pandas (数据处理)
- nginx (Web服务器)

## 备份策略

- Excel文件自动备份（保留7天）
- nginx配置自动备份
- 备份位置：`/root/nginx_backup_日期时间/`
