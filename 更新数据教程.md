# 📤 审批数据更新教程

## 🎯 适用人群
不懂代码的人也能轻松更新数据！

---

## 方法一：Windows 一键更新（推荐）

### 📋 准备工作（只需做一次）

1. **安装 Python**
   - 下载地址：https://www.python.org/downloads/
   - 安装时勾选 "Add Python to PATH"

2. **安装 Git for Windows**（用于上传文件）
   - 下载地址：https://git-scm.com/download/win
   - 一路点"下一步"安装即可

### 🚀 更新步骤

1. **双击运行** `一键更新数据.bat`

2. **拖入Excel文件**
   - 把新下载的Excel文件拖到黑色窗口
   - 按回车键

3. **首次使用需要输入服务器信息**
   - 服务器IP：你的服务器IP地址
   - 用户名：通常是 `root`
   - 密码：输入时不显示，输完直接按回车

4. **等待完成**
   - 看到"数据更新成功"就完成了
   - 刷新网页（按 Ctrl+F5）查看最新数据

### ⚠️ 常见问题

**Q: 提示"未检测到Python"**
- A: 需要先安装Python，见上面的准备工作

**Q: 提示"未找到scp命令"**
- A: 需要安装Git for Windows，见上面的准备工作

**Q: 上传失败**
- A: 检查服务器IP是否正确，是否能SSH连接

---

## 方法二：手动上传（备用方案）

如果方法一不行，可以用这个方法：

### 步骤1：生成数据文件

在本地电脑运行：
```bash
python parse_excel.py 新下载的审批数据.xlsx
```

会生成 `approval_data.json` 文件

### 步骤2：上传到服务器

使用 FTP 工具（如 FileZilla）或 SCP 命令：

```bash
scp approval_data.json root@你的服务器IP:/var/www/approval-viewer/
```

### 步骤3：检查权限

SSH 登录服务器后运行：
```bash
cd /var/www/approval-viewer
chmod 644 approval_data.json
```

---

## 方法三：直接在服务器上更新

### 步骤1：上传Excel文件到服务器

使用 FTP 工具上传到服务器的任意位置，比如 `/root/`

### 步骤2：SSH登录服务器

```bash
ssh root@你的服务器IP
```

### 步骤3：运行更新脚本

```bash
cd /var/www/approval-viewer
bash daily_update.sh /root/新审批数据.xlsx
```

---

## 🔍 验证数据是否更新成功

1. **检查文件是否存在**
   ```bash
   ls -lh /var/www/approval-viewer/approval_data.json
   ```

2. **查看文件内容**
   ```bash
   head -20 /var/www/approval-viewer/approval_data.json
   ```

3. **访问网页**
   - 打开：http://blitzepanda.top/approvalquery
   - 按 Ctrl+F5 强制刷新
   - 检查数据是否是最新的

---

## 🛠️ 故障排查

### 问题1：网页显示"数据加载失败"

**原因**：`approval_data.json` 文件不存在或路径不对

**解决方法**：
```bash
# SSH登录服务器
cd /var/www/approval-viewer

# 检查文件是否存在
ls -lh approval_data.json

# 如果不存在，重新生成
python3 parse_excel.py

# 检查文件权限
chmod 644 approval_data.json
```

### 问题2：nginx 配置问题

**检查配置**：
```bash
cat /etc/nginx/sites-available/approval-viewer
```

**应该包含**：
```nginx
location /approvalquery {
    alias /var/www/approval-viewer;
    index index.html;
    try_files $uri $uri/ /approvalquery/index.html;
}
```

**重启nginx**：
```bash
sudo nginx -t
sudo systemctl reload nginx
```

### 问题3：文件权限问题

```bash
cd /var/www/approval-viewer
sudo chown -R www-data:www-data .
sudo chmod 644 *.json *.html
sudo chmod 755 .
```

---

## 📞 需要帮助？

如果遇到问题：

1. 检查服务器上文件是否存在：
   ```bash
   ls -lh /var/www/approval-viewer/approval_data.json
   ```

2. 查看nginx错误日志：
   ```bash
   sudo tail -50 /var/log/nginx/error.log
   ```

3. 测试文件是否可访问：
   ```bash
   curl http://localhost/approvalquery/approval_data.json
   ```
