# 🚀 审批系统 - 超简单操作指南

## 😱 遇到"数据加载失败"？

### 立即修复（在服务器上运行）

```bash
ssh root@你的服务器IP
cd /var/www/approval-viewer
bash 快速修复.sh
```

完成！刷新网页就能看到数据了。

---

## 📤 日常更新数据（3种方法任选）

### 方法1：Windows 一键更新 ⭐推荐

**适合：不懂代码的人**

1. 双击 `一键更新数据.bat`
2. 拖入新的Excel文件
3. 按回车，等待完成
4. 刷新网页

**首次使用需要安装：**
- Python: https://www.python.org/downloads/
- Git for Windows: https://git-scm.com/download/win

---

### 方法2：服务器端更新

**适合：会用SSH的人**

```bash
# 1. 上传Excel到服务器
scp 新审批数据.xlsx root@服务器IP:/root/

# 2. SSH登录服务器
ssh root@服务器IP

# 3. 运行更新脚本
cd /var/www/approval-viewer
bash daily_update.sh /root/新审批数据.xlsx
```

---

### 方法3：手动上传JSON

**适合：只想上传文件的人**

```bash
# 1. 本地生成JSON
python parse_excel.py 新审批数据.xlsx

# 2. 上传到服务器
scp approval_data.json root@服务器IP:/var/www/approval-viewer/

# 3. SSH登录设置权限
ssh root@服务器IP
cd /var/www/approval-viewer
chmod 644 approval_data.json
```

---

## 🔍 检查系统状态

```bash
ssh root@服务器IP
cd /var/www/approval-viewer
bash 检查部署状态.sh
```

会自动检查所有配置，告诉你哪里有问题。

---

## ⚠️ 常见问题

### Q1: 网页显示"数据加载失败"

**原因**：服务器上没有 `approval_data.json` 文件

**解决**：
```bash
ssh root@服务器IP
cd /var/www/approval-viewer
bash 快速修复.sh
```

### Q2: Windows批处理提示"未检测到Python"

**解决**：安装Python
- 下载：https://www.python.org/downloads/
- 安装时勾选 "Add Python to PATH"

### Q3: 提示"未找到scp命令"

**解决**：安装Git for Windows
- 下载：https://git-scm.com/download/win
- 一路点"下一步"安装

### Q4: 上传失败

**检查**：
1. 服务器IP是否正确
2. 能否SSH连接：`ssh root@服务器IP`
3. 密码是否正确

---

## 📁 重要文件说明

| 文件 | 用途 |
|------|------|
| `一键更新数据.bat` | Windows一键更新脚本 |
| `快速修复.sh` | 服务器端快速修复脚本 |
| `检查部署状态.sh` | 检查系统状态 |
| `daily_update.sh` | 服务器端更新脚本 |
| `parse_excel.py` | Excel转JSON工具 |
| `approval_data.json` | 网页需要的数据文件 |
| `index.html` | 网页文件 |

---

## 🌐 访问地址

http://blitzepanda.top/approvalquery

**刷新页面**：按 `Ctrl + F5` 强制刷新

---

## 📞 需要帮助？

1. 运行检查脚本看看哪里有问题：
   ```bash
   bash 检查部署状态.sh
   ```

2. 查看nginx日志：
   ```bash
   sudo tail -50 /var/log/nginx/error.log
   ```

3. 测试文件是否存在：
   ```bash
   ls -lh /var/www/approval-viewer/approval_data.json
   ```
