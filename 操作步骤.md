# 🚀 立即操作步骤

## 第一步：提交代码

```bash
# 在本地（Windows）
git add .
git commit -m "清理项目，简化配置"
git push
```

## 第二步：服务器更新

```bash
# SSH登录服务器
ssh root@139.224.200.133

# 进入项目目录
cd /var/www/approval-viewer/approvalquery

# 拉取最新代码
git pull

# 设置脚本执行权限
chmod +x *.sh
```

## 第三步：配置nginx（智能检测所有项目）

```bash
# 运行智能检测和配置脚本
bash 检测并配置nginx.sh
```

这个脚本会：
- ✅ 自动搜索 /var/www, /opt, /home 目录
- ✅ 智能检测 approvalquery, kart, wuliu, weeklycheck
- ✅ 显示每个项目的路径和大小
- ✅ 生成统一的nginx配置
- ✅ 支持域名和IP访问
- ✅ 自动备份旧配置

## 第四步：修复数据加载

```bash
# 确保数据文件正确
bash 修复数据加载.sh
```

## 第五步：验证

浏览器访问测试：
- http://blitzepanda.top/approvalquery/
- http://139.224.200.133/approvalquery/
- http://blitzepanda.top/kart/
- http://blitzepanda.top/wuliu/
- http://blitzepanda.top/weeklycheck/

按 `Ctrl+F5` 强制刷新。

## 如果遇到问题

### 问题1：找不到某些项目

脚本会自动搜索 `/var/www`, `/opt`, `/home` 目录。

如果还是找不到，手动搜索：
```bash
find /var/www /opt /home -name "index.html" -type f 2>/dev/null
```

把结果发给我。

### 问题2：数据加载失败

```bash
bash 修复数据加载.sh
```

如果还不行，按 F12 打开浏览器控制台，把错误信息发给我。

### 问题3：nginx配置错误

```bash
# 查看错误
sudo nginx -t

# 查看日志
sudo tail -50 /var/log/nginx/error.log
```

## 清理后的文件列表

```
✅ 保留的核心文件：
- index.html
- parse_excel.py
- approval_data.json
- 线上建店审批.xlsx
- requirements.txt

✅ 保留的脚本：
- 一键更新数据.bat (Windows用)
- daily_update.sh (服务器更新)
- 修复数据加载.sh (故障修复)
- 检测并配置nginx.sh (智能检测nginx)
- 服务器部署.sh (首次部署)

✅ 保留的文档：
- README.md (项目说明)
- 使用指南.md (使用方法)
- 项目结构.md (结构说明)
- 操作步骤.md (本文件)

❌ 已删除：
- 所有临时诊断脚本
- 所有啰嗦的文档
- 重复的修复脚本
- 旧的部署脚本
```

## 下次更新数据

**Windows用户：**
```
双击 一键更新数据.bat
```

**服务器端：**
```bash
bash daily_update.sh 新Excel文件.xlsx
```

简单！🎉
