# 云服务器操作指南

## 🚀 快速操作（最常用）

### 场景1：更新Excel数据

**支持两种Excel格式：**
- `线上建店审批.xlsx` - 企微审批导出
- `线上建店进度.xlsx` - 建店进度表
- 脚本自动识别格式

```bash
# 1. SSH登录
ssh root@139.224.200.133

# 2. 进入项目目录
cd /var/www/approval-viewer/approvalquery

# 3. 上传新Excel（用FTP或scp上传到这个目录）
# 确保文件名是：线上建店审批.xlsx

# 4. 生成数据
python3 parse_excel.py

# 5. 设置权限
chmod 644 approval_data.json

# 完成！刷新浏览器查看
```

**一行命令版本：**
```bash
cd /var/www/approval-viewer/approvalquery && python3 parse_excel.py && chmod 644 approval_data.json
```

---

## 📋 详细步骤

### 第一步：上传Excel到服务器

**方法1：使用scp（从本地电脑）**
```bash
scp 线上建店审批.xlsx root@139.224.200.133:/var/www/approval-viewer/approvalquery/
```

**方法2：使用FTP工具**
- 用FileZilla等工具
- 上传到：`/var/www/approval-viewer/approvalquery/`
- 文件名：`线上建店审批.xlsx`

**方法3：直接在服务器上下载**
```bash
cd /var/www/approval-viewer/approvalquery
# 如果Excel在某个网盘，用wget下载
wget "下载链接" -O 线上建店审批.xlsx
```

### 第二步：SSH登录服务器

```bash
ssh root@139.224.200.133
```

### 第三步：进入项目目录

```bash
cd /var/www/approval-viewer/approvalquery
```

### 第四步：查看文件

```bash
# 查看当前目录文件
ls -lh

# 应该能看到：
# - 线上建店审批.xlsx
# - parse_excel.py
# - index.html
```

### 第五步：生成数据

```bash
python3 parse_excel.py
```

**输出示例：**
```
✅ 数据解析完成！
总计: 126 个审批
已通过: 75 个
审批中: 33 个
已驳回: 2 个
已撤销: 16 个

数据已保存到: approval_data.json
```

### 第六步：设置权限

```bash
chmod 644 approval_data.json
```

### 第七步：验证

```bash
# 查看生成的文件
ls -lh approval_data.json

# 测试访问
curl -I http://localhost/approvalquery/approval_data.json
# 应该返回 HTTP/1.1 200 OK
```

### 第八步：浏览器查看

访问：http://blitzepanda.top/approvalquery

按 `Ctrl+F5` 强制刷新

---

## 🔧 使用更新脚本（推荐）

如果你有多个Excel文件，使用脚本更方便：

```bash
# 进入目录
cd /var/www/approval-viewer/approvalquery

# 运行更新脚本
bash daily_update.sh 线上建店审批.xlsx
```

脚本会自动：
- ✅ 备份旧文件
- ✅ 生成新数据
- ✅ 设置权限
- ✅ 清理旧备份

---

## 🛠️ 常用命令

### 查看当前数据

```bash
cd /var/www/approval-viewer/approvalquery

# 查看JSON文件大小
ls -lh approval_data.json

# 查看数据统计（前20行）
head -20 approval_data.json
```

### 查看备份文件

```bash
cd /var/www/approval-viewer/approvalquery

# 列出所有备份
ls -lh *_backup_*.xlsx
```

### 删除旧备份

```bash
cd /var/www/approval-viewer/approvalquery

# 删除7天前的备份
find . -name "*_backup_*.xlsx" -mtime +7 -delete
```

### 测试网页访问

```bash
# 测试JSON文件
curl -I http://localhost/approvalquery/approval_data.json

# 测试HTML页面
curl -I http://localhost/approvalquery/

# 应该都返回 HTTP/1.1 200 OK
```

### 查看nginx日志

```bash
# 查看错误日志
sudo tail -50 /var/log/nginx/error.log

# 查看访问日志
sudo tail -50 /var/log/nginx/access.log
```

---

## 🔄 完整工作流程

### 日常更新数据

```bash
# 1. 上传新Excel（用FTP或scp）
scp 线上建店审批.xlsx root@139.224.200.133:/var/www/approval-viewer/approvalquery/

# 2. SSH登录
ssh root@139.224.200.133

# 3. 一行命令更新
cd /var/www/approval-viewer/approvalquery && python3 parse_excel.py && chmod 644 approval_data.json

# 4. 验证
curl -I http://localhost/approvalquery/approval_data.json

# 完成！
```

### 首次部署

```bash
# 1. SSH登录
ssh root@139.224.200.133

# 2. 进入目录
cd /var/www/approval-viewer/approvalquery

# 3. 拉取最新代码
git pull

# 4. 设置脚本权限
chmod +x *.sh

# 5. 配置nginx
bash 检测并配置nginx.sh

# 6. 生成数据
python3 parse_excel.py

# 7. 设置权限
chmod 644 approval_data.json

# 完成！
```

---

## 📞 故障排查

### 问题1：提示"找不到parse_excel.py"

```bash
# 检查当前目录
pwd
# 应该显示：/var/www/approval-viewer/approvalquery

# 如果不对，进入正确目录
cd /var/www/approval-viewer/approvalquery
```

### 问题2：提示"No module named 'openpyxl'"

```bash
# 安装依赖
pip3 install openpyxl pandas
```

### 问题3：生成的JSON文件为空

```bash
# 检查Excel文件是否存在
ls -lh 线上建店审批.xlsx

# 重新生成
python3 parse_excel.py
```

### 问题4：网页显示"数据加载失败"

```bash
# 运行修复脚本
bash 修复数据加载.sh
```

---

## 💡 小技巧

### 快速命令别名

在 `~/.bashrc` 添加：

```bash
alias update-approval='cd /var/www/approval-viewer/approvalquery && python3 parse_excel.py && chmod 644 approval_data.json'
```

然后：
```bash
source ~/.bashrc
```

以后只需运行：
```bash
update-approval
```

### 自动化脚本

创建 `/root/update.sh`：

```bash
#!/bin/bash
cd /var/www/approval-viewer/approvalquery
python3 parse_excel.py
chmod 644 approval_data.json
echo "✅ 更新完成！"
```

使用：
```bash
bash /root/update.sh
```

---

## 🎯 最简操作流程

**如果Excel已经在服务器上：**

```bash
ssh root@139.224.200.133
cd /var/www/approval-viewer/approvalquery
python3 parse_excel.py
chmod 644 approval_data.json
```

**就这4行命令！** 🎉
